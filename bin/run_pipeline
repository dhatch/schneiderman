#!/bin/bash

DIR=`dirname $0`

if [ $# -lt 2 ]; then
    echo "run_pipeline: <salary_file> <date>" >&2
    exit 1
fi

set -e

source $DIR/set_env
cd $DIR

./scrape
./load_db
./clean
./train
./todays_games $1 $2
./predict
./set_lineup
